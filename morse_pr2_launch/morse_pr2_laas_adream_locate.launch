<launch>
  <!-- start map_server with laas_adream map -->
  <!-- <node name="map_server" pkg="map_server" type="map_server" args="$(find laas_navigation)/maps/laas_adream_new.yaml"/> -->
  <!-- <node name="map_server" pkg="map_server" type="map_server" args="$(find laas_navigation)/maps/crowd_new.yaml"/> -->
  <node name="map_server" pkg="map_server" type="map_server" args="$(find laas_navigation)/maps/narrow.yaml"/>

  <!-- start acml for localization -->
  <!-- <node name="amcl" pkg="amcl" type="amcl">
    <param name="initial_pose_x" value="2.0"/>
    <param name="initial_pose_y" value="2.0"/>
    <param name="initial_pose_a" value="0.0"/>
  </node> -->

</launch>
